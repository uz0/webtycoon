.manager
  .tab(
    *ngFor='let current of site.list',
    [ngClass]="{'active':current==site.active}",
    (click)='site.setActive(current)'
  )
    | {{ current.name }}
  .tab.plus(*ngIf='!isEditing', (click)='isEditing = true')
    | +
  form.new-site(*ngIf='isEditing', (ngSubmit)='saveNewSite()')
    input.tab(type='text', [(ngModel)]='sitename', name='sitename', placeholder = 'Your site\'s name goes here')
    button.tab([disabled]='!sitename') Ok
  
.content
  .sidebar
    ul
      li(*ngFor='let advert of site.active.adverts')
        | {{advert}}
  .mainblock
    h2(*ngIf='site.active.name') {{site.active.name}}
    ul
      li(*ngFor='let article of site.active.articles')
        | {{article}}
